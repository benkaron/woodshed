#!/bin/bash
# Start Woodshed dev environment
# Usage: ./dev

trap 'kill 0' EXIT

echo "Starting Woodshed..."
echo "  Backend  → http://localhost:8000"
echo "  Frontend → http://localhost:5173"
echo ""

cd backend && uv run uvicorn app.main:app --reload &
cd frontend && npm run dev &

wait
